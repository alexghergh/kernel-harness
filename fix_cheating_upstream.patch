diff --git a/src/kernelbench/eval.py b/src/kernelbench/eval.py
index 3557dc9..a2c5dfc 100644
--- a/src/kernelbench/eval.py
+++ b/src/kernelbench/eval.py
@@ -774,13 +774,19 @@ def run_and_check_correctness(
      
             model_new = new_model_instance.to(device=device, dtype=precision)
 
-            output = model(*inputs)
-            torch.cuda.synchronize(device=device)
-            # ensure all GPU operations are completed before checking results
-
+            # run the candidate kernel first so it cannot reuse memory
+            # populated by the reference (see issue #38)
+            # clone inputs so an in-place mutation in model_new cannot
+            # corrupt the reference run
+            inputs_ref = [
+                x.clone() if isinstance(x, torch.Tensor) else x for x in inputs
+            ]
             try:
                 output_new = model_new(*inputs)
                 torch.cuda.synchronize(device=device)
+
+                output = model(*inputs_ref)
+                torch.cuda.synchronize(device=device)
                 if output.shape != output_new.shape:
                     metadata = register_and_format_exception(
                         "correctness_issue",
